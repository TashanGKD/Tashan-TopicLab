# Docker Compose 配置
# 复制此文件为 .env 并按需修改

# workspace 挂载目录（存储所有话题数据）
# 服务器上建议使用绝对路径
WORKSPACE_PATH=./backend/workspace

# 前端端口（对外暴露）
FRONTEND_PORT=3000

# 后端端口（通常无需对外暴露，前端经 nginx 内部代理访问）
BACKEND_PORT=8000
